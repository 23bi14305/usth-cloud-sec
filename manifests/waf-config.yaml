apiVersion: v1
kind: ConfigMap
metadata:
  name: waf-config
data:
  default.conf: |
    server {
        listen 80;
        location / {
            proxy_pass http://dvwa-web:80; 
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;

            modsecurity on;
            modsecurity_rules_file /etc/nginx/modsec/main.conf;
        }
    }
